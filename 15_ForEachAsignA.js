const ArrayNumbers=[1,-7,40,502,-77,91,0,108,89,-601]

console.log(`*********************************************Array Elements With its Index***********************************************************`);
ArrayNumbers. forEach(function(currentValue,index){
    console.log(`Array Element:${currentValue},   Array Index: ${index}`);
})
console.log(`*********************************************To Find The Positive Numbers From Given Array***********************************************************`);
ArrayNumbers. forEach(element=>{
    if (element>=0) {
        
    }
    console.log(`Array Positive Elements:${element},   `);

})